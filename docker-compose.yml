version: '3.8'
services:
  app:
    build: .
    ports:
      - '8000:8000'
    environment:
      - DATABASE_URL=sqlite:///./test.db
  db:
    image: sqlite:latest
    volumes:
      - db_data:/data
volumes:
  db_data: